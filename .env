# Port for the FastMCP server (default: 8978)
FASTMCP_PORT=8978

# Path to your projects root folder (absolute path recommended)
# Example: PROJECTS_ROOT=~/proj/ai-experiments
PROJECTS_ROOT=~/git

# Comma-separated list of folder names to index under PROJECTS_ROOT
# Example: FOLDERS_TO_INDEX=project1,project2,project3
# Leave empty to auto-discover all folders in PROJECTS_ROOT
FOLDERS_TO_INDEX=

# Comma-separated list of additional directories to ignore beyond defaults
# Example: ADDITIONAL_IGNORE_DIRS=build,dist,coverage
ADDITIONAL_IGNORE_DIRS=vendor,third_party,Godeps,_vendor,external,dependencies,_output,dist,build,.cache,bin,out,target,node_modules

# Comma-separated list of additional files to ignore beyond defaults
# Example: ADDITIONAL_IGNORE_FILES=custom.log,temp.txt
ADDITIONAL_IGNORE_FILES=*.pb.go,*.generated.go,*_generated.go,*.min.js,*.bundle.js

# Debounce time in seconds for file change processing (default: 5.0)
# Increase for large repos with frequent git operations (e.g., 10-15 seconds)
# File changes are batched and deduplicated during this window
DEBOUNCE_SECONDS=10.0